# /Rejected — Rejected Actions

Move approval files here to cancel the action. Claude will log the rejection and mark the related task as `status: rejected` in `/Done/`.

No action script is called for rejected files.
